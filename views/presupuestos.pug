extends layout

block content
  h1.mb-4 Lista de Presupuestos

  if presupuestos.length
    table.table.table-striped
      thead
        tr
          th Nombre
          th Evento
          th Fecha de Emisión
          th Estado
          th Total
          th Acciones
      tbody
        each presupuesto in presupuestos
          tr
            td= presupuesto.nombre
            td= presupuesto.eventoNombre
            td= new Date(presupuesto.fechaEmision).toLocaleDateString('es-AR')
            td= presupuesto.estado
            td $#{presupuesto.totalGeneral.toFixed(2)}
            td
              a.btn.btn-sm.btn-info(href=`/presupuestos/vista/${presupuesto._id}?from=listado`) Ver              
  else
    p.text-muted No hay presupuestos cargados aún.

  a.btn.btn-secondary.mt-3(href='/home') Volver

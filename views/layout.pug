html(lang="es")
  head
    meta(charset="utf-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title= titulo || "Eventify"
    link(rel="stylesheet", href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css")

  body(class="bg-light")

    nav.navbar.navbar-expand-lg.navbar-dark.bg-dark.mb-4
      .container-fluid
        if user
          a.navbar-brand(href="/home") Eventify
          ul.navbar-nav.me-auto.mb-2.mb-lg-0
            li.nav-item
              a.nav-link(href="/eventos/vista") Eventos
            li.nav-item
              a.nav-link(href="/clientes/vista") Clientes
            li.nav-item
              a.nav-link(href="/proveedores/vista") Proveedores
            li.nav-item
              a.nav-link(href="/tareas/vista") Tareas
            li.nav-item
              a.nav-link(href="/presupuestos/vista") Presupuestos
            li.nav-item
              a.nav-link(href="/reportes/vista") Reportes
            if user && user.role === "admin"
              li.nav-item
                a.nav-link(href="/signup") Nuevo usuario


        ul.navbar-nav.ms-auto
          if user
            li.nav-item
              span.nav-link.text-white= user.username
            li.nav-item
              a.nav-link(href="/signout") Cerrar sesión


    .container.mt-4
      block content

    footer.text-center.mt-5.py-3.bg-dark.text-white
      p &copy; #{new Date().getFullYear()} - Eventify App
      script(src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js")
